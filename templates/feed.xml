<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
<title>eduxfeed</title>

{% for item in items %}
<entry>
    <title>{{ item.code }}: {{ item.path | path }}</title>
    <link rel="alternate" type="text/html" href="{{ item | link(username) }}"/>
    # src code path item
    <author><name>{{ item.item.updates[item.item.to] | author | e }}</name></author>
    <content type="html">
        <p>
        {% if item.item.new %}
            New {% if item.src == 'pages' %}page{% else %}file{% endif %} created.
        {% else %}
            {% if item.src == 'pages' %}Page{% else %}File{% endif %} updated.
        {% endif %}
        </p>
        
        <p>Update time: {{ item.item.updates[item.item.to] | time | e }}</p>
        <p><i>
        {% if item.src == 'pages' %}
            Edits:
            </br>
            {% for timestamp in item.item.updates | sort %}
                {{ item.item.updates[timestamp] | time | e }} {{ item.item.updates[timestamp] | author | e }}</br>
            {% endfor %}
        {% else %}
            Filesize: {{ item.item.updates[item.item.to] | size | e }}
        {% endif %} 
        </i></p>
        
        <p>
            <a href="{{ item | link(username, target='current') }}">Open current version</a>
        {% if item.src == 'pages' %}
            </br>
            <a href="{{ item | link(username, target='compare') }}">See the changes</a>
        {% endif %}
        </p>
    </content>
</entry>
{% endfor %}
